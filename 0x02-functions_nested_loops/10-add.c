#include "main.h"

/**
 * add - adds two integers and returns the result.
 * a - first number
 * b - second number
 * Result: sum of two numbers
 * Return: 0
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	_putchar(result + '0');
	return (result);
}
